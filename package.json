{
  "name": "clipchef",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:start": "npm start --prefix frontend",
    "frontend:install": "npm install --prefix frontend",
    "backend:build": "cd backend && sam build",
    "backend:start": "cd backend && sam local start-api",
    "backend:deploy": "cd backend && sam deploy"
  }
}
